import React from 'react';
import MatchParams from './interfaces/matchParams.interface';

const Project: React.FC<MatchParams> = (props) => {
  console.log(props);

  return (
    <div>
      Teste
    </div>
  );
}

export default Project;
